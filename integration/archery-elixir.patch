diff --git a/dev/archery/archery/cli.py b/dev/archery/archery/cli.py
index e70cc38..dbcb923 100644
--- a/dev/archery/archery/cli.py
+++ b/dev/archery/archery/cli.py
@@ -690,6 +690,9 @@ def _set_default(opt, default):
 @click.option('--with-rust', type=bool, default=False,
               help='Include Rust in integration tests',
               envvar="ARCHERY_INTEGRATION_WITH_RUST")
+@click.option('--with-elixir', type=bool, default=False,
+              help='Include Elixir in integration tests',
+              envvar="ARCHERY_INTEGRATION_WITH_ELIXIR")
 @click.option('--target-implementations', default='',
               help=('Target implementations in this integration tests'),
               envvar="ARCHERY_INTEGRATION_TARGET_IMPLEMENTATIONS")
diff --git a/dev/archery/archery/integration/datagen.py b/dev/archery/archery/integration/datagen.py
index 3dbab0d..64c6f61 100644
--- a/dev/archery/archery/integration/datagen.py
+++ b/dev/archery/archery/integration/datagen.py
@@ -1967,7 +1967,9 @@ def get_generated_json_files(tempdir=None):
         generate_nested_large_offsets_case()
         .skip_tester('JS'),
 
-        generate_unions_case(),
+        generate_unions_case()
+        # Union types not supported by the Elixir implementation
+        .skip_tester('Elixir'),
 
         generate_custom_metadata_case(),
 
@@ -1999,20 +2001,26 @@ def get_generated_json_files(tempdir=None):
         .skip_tester('JS')
         # TODO(https://github.com/apache/arrow-nanoarrow/issues/618)
         .skip_tester('nanoarrow')
-        .skip_tester('Ruby'),
+        .skip_tester('Ruby')
+        # Run-end encoding not supported by the Elixir implementation
+        .skip_tester('Elixir'),
 
         generate_binary_view_case()
         .skip_tester('JS')
         # TODO(https://github.com/apache/arrow-nanoarrow/issues/618)
         .skip_tester('nanoarrow')
-        .skip_tester('Ruby'),
+        .skip_tester('Ruby')
+        # View types not supported by the Elixir implementation
+        .skip_tester('Elixir'),
 
         generate_list_view_case()
         .skip_tester('.NET')     # Doesn't support large list views
         .skip_tester('JS')
         # TODO(https://github.com/apache/arrow-nanoarrow/issues/618)
         .skip_tester('nanoarrow')
-        .skip_tester('Ruby'),
+        .skip_tester('Ruby')
+        # View types not supported by the Elixir implementation
+        .skip_tester('Elixir'),
 
         generate_extension_case()
         # Also contains a dictionary column
diff --git a/dev/archery/archery/integration/runner.py b/dev/archery/archery/integration/runner.py
index 9c0fda3..7b136f4 100644
--- a/dev/archery/archery/integration/runner.py
+++ b/dev/archery/archery/integration/runner.py
@@ -594,6 +594,7 @@ def get_static_json_files():
 def select_testers(with_cpp=True, with_java=True, with_js=True,
                    with_dotnet=True, with_go=True, with_ruby=False,
                    with_rust=False, with_nanoarrow=False,
+                   with_elixir=False,
                    target_implementations="", **kwargs):
     target_implementations = (target_implementations.split(",")
                               if target_implementations else [])
@@ -639,6 +640,10 @@ def select_testers(with_cpp=True, with_java=True, with_js=True,
         from .tester_rust import RustTester
         append_tester("rust", RustTester(**kwargs))
 
+    if with_elixir:
+        from .tester_elixir import ElixirTester
+        append_tester("elixir", ElixirTester(**kwargs))
+
     return testers, other_testers
 
 
